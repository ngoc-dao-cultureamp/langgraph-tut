# Copy this file to local.env and set the path to your llama-server binary.
# local.env is gitignored — machine-local overrides only.
# Use full paths or $HOME — tilde (~) is not expanded by /bin/sh.

LLAMA_SERVER_BIN=$HOME/src/llama.cpp/build/bin/llama-server
